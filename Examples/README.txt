***WARNING***
Anaconda 3 must be installed to execute!


****STEPS***
1. Replace "Estres_UVM.csv" file with the ".csv" file you want to convert.
2. Open the "ExportDataframeSeriesToCategoryCodes.py" script to indicate the data rows that will be excluded to convert (see in code line 6).
2. Double click on "ExecuteProgram.bat" or run a .CMD and to drag and drop the "ExecuteProgram.bat" and press Enter to execute. 